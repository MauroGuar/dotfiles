local P = {
    general = require("modules.general"),
    rules = require("modules.rules"),
    keybinds = require("modules.keybinds"),
    workspaces = require("modules.workspaces"),
    monitors = require("modules.inputAndMonitors"),
    lookAndFeel = require("modules.lookAndFeel"),
    autostart = require("modules.autostart")
}

return P
